@model ProductViewModel.Models.ProductIndexViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var product in Model.Products)
{
    <li>
        @product.Name @product.Price
    </li>
}

